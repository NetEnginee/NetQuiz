RewriteEngine On

# Blokir akses langsung ke folder core
RewriteRule ^(app|config|logs)/ - [F,L]

# Arahkan semua request domain utama ke folder public
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L]